# ... existing imports ...
INSTALLED_APPS = [
     'rest_framework',
    # Your API app
    'api',
]

# Using default SQLite DB (no changes needed unless you want another DB)
# ... rest of the settings file ...
